export type slackDetails = {
  access_token: string;
  bot_user_id: string;
  channel_id: string;
  created_at: string;
  id: number;
  project_id: string;
  scope: string;
  team_id: string;
  team_name: string;
};
